main
num V_a, num V_b, num V_c, num V_result,
begin
  V_a = 10;
  V_b = 5;
  V_c = 2;
  V_result = add(mul(V_a, V_b), div(sub(V_c, V_a), V_b));
  print V_result;
  V_result = F_complex(V_a, V_b, V_c);
  print V_result;
end

num F_complex(num V_x, num V_y, num V_z)
{
  num V_temp,
  begin
    V_temp = add(mul(V_x, V_y), div(V_z, 2));
    return sqrt(add(mul(V_temp, V_temp), V_z));
  end
}